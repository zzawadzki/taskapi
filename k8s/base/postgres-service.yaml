# Service for PostgreSQL database
# Provides stable internal network endpoint for database access
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: taskapi
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: postgres
