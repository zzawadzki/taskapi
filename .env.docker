# Database Configuration for Docker
POSTGRES_DB=taskapi_db
POSTGRES_USER=taskapi_user
POSTGRES_PASSWORD=local_dev_password

# Spring Boot Database Connection (use these when running the app locally)
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/taskapi_db
SPRING_DATASOURCE_USERNAME=taskapi_user
SPRING_DATASOURCE_PASSWORD=local_dev_password

# JWT Configuration (generate a secure key for production)
JWT_SECRET=bXlTdXBlclNlY3VyZUpXVFNlY3JldEtleUZvckRldmVsb3BtZW50T25seQ==
JWT_EXPIRATION=86400000
